<%= form_for(@task) do |f| %>

  <% if @task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="form-group">
    <%= f.label :description, class: "col-sm-1" %>
  <div class="col-sm-10">
    <%= f.text_field :description, class: "col-sm-8" %>
  </div>
</div>

<br>
<br>





<div class="form-group">
    <%= f.label :date, class: "col-sm-1" %>
  <div class="col-sm-10">
    <%= f.date_field :date, class: "col-sm-3" %>
<div>

<br>
<br>

<div class="form-group">
    <div class="left">
      <br>
          <%= f.check_box :complete %>
          <%= f.label :complete %>
    </div>
</div>

<div class="form-group">
    <div class="left">
      <%= f.submit "Update Task", class: "btn btn-primary"%>
    </div>
</div>

<% end %>
