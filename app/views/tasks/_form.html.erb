<%= form_for(@task, html: {class: "form-horizontal"}) do |t| %>

  <h1><%= title %></h1>

  <%= render "shared/errors", error_for: @task %>

  <div class="form-group">
      <%= t.label :description, class: "col-sm-1 control-label" %>
    <div class="col-sm-3">
      <%= t.text_field :description, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
      <%= t.label :due_date, class: "col-sm-1 control-label" %>
    <div class="col-sm-2">
      <%= t.date_field :due_date, class: "col-sm-3 form-control" %>
    </div>
  </div>

  <div class="form-group col-sm-offset-1 col-sm-11">
    <% if @task.id %>
      <div class="left">
          <br>
          <%= t.check_box :complete %>
          <%= t.label :complete %>
      </div>
    <% end %>
    <br>
    <div class="left">
        <%= t.submit class: "btn btn-primary btn-info"%>
    </div>
  </div>

<% end %>
