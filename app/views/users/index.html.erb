<strong><p id="notice" class="text-center"><%= notice %></p></strong>



<div class= "container tasks_page">
  <table class="table">
    <h1>Users
        &nbsp
        <%= link_to 'Create User', new_user_path, class: "btn btn-primary pull-right" %>
      </div>
    </h1>

    <hr>

<table class="table">
    <thead>
      <tr>
        <th>Name</th>
        <th>Email</th>
      </tr>
    </thead>
    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.first_name %>&nbsp<%= user.last_name %></td>
          <td><%= mail_to user.email %></td>
          <td><%= link_to 'Edit', edit_user_path(user), class: "pull-right" %></td>
        </tr>
      <% end %>
    </tbody>

  </table>

    <br>
    <br>
    <br>
    <br>

  </table>
</div>
